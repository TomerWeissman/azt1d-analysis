[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "azt1d"
version = "0.1.0"
description = "Loading and EDA helpers for the AZT1D type-1-diabetes dataset"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
